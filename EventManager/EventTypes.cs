namespace EventTypes
{
    public enum EventType1
    {
        
    }

    public enum EventType2
    {
        
    }
    
    // Add ad nauseam. As long as the Enums are defined within the EventTypes namespace, they will be included in the dictionary initialization. 
}